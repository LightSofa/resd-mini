  # 0) 进入项目目录
  Set-Location E:\project\resd-mini

  # 1) 设置连接变量
  $KEY = "\\iStoreOS\admin_home\.ssh\id_ed25519"
  $ROUTER = "root@192.168.1.254"

  # 2) 查看网关架构（决定 GOARCH）
  ssh -i $KEY $ROUTER "uname -m"

  uname -m 常见对应：

  - aarch64 -> GOARCH=arm64
  - armv7l -> GOARCH=arm 且 GOARM=7

  # 3) 本地编译（按你的架构二选一）
  New-Item -ItemType Directory -Force .\dist | Out-Null

  go build -trimpath -ldflags "-s -w" -o .\dist\resd-mini .

  # 3B: armv7l（如果是 armv7l 才用）
  # $env:CGO_ENABLED="0"; $env:GOOS="linux"; $env:GOARCH="arm"; $env:GOARM="7"
  # go build -trimpath -ldflags "-s -w" -o .\dist\resd-mini .

  # 4) 上传二进制和服务脚本
  scp -i $KEY .\dist\resd-mini $ROUTER:/usr/bin/resd-mini
  scp -i $KEY .\build\istoreos\resd-mini.init $ROUTER:/etc/init.d/resd-mini
  scp -i $KEY .\build\istoreos\resd-mini.config.example $ROUTER:/etc/config/resd-mini

  # 5) 设置开机自启并启动
  ssh -i $KEY $ROUTER "chmod +x /etc/init.d/resd-mini && mkdir -p /root/downloads/resd-mini && /etc/init.d/resd-mini enable && /etc/init.d/resd-mini restart"

  # 6) 可选：用 UCI 改监听地址/端口/保存目录（默认已是 0.0.0.0:8899）
  ssh -i $KEY $ROUTER "uci set resd-mini.main.host='0.0.0.0'; uci set resd-mini.main.port='8899'; uci set resd-mini.main.save_dir='/root/downloads/resd-mini'; uci commit resd-mini; /
  etc/init.d/resd-mini restart"

  # 7) 健康检查（网关本机）
  ssh -i $KEY $ROUTER "wget -qO- http://127.0.0.1:8899/api/v1/health"

  # 8) 健康检查（你的电脑直连网关）
  curl.exe http://192.168.1.254:8899/api/v1/health

  # 9) 查看运行日志
  ssh -i $KEY $ROUTER "logread -f | grep resd-mini"

  # 10) 常用服务管理
  ssh -i $KEY $ROUTER "/etc/init.d/resd-mini status"
  ssh -i $KEY $ROUTER "/etc/init.d/resd-mini restart"
  ssh -i $KEY $ROUTER "/etc/init.d/resd-mini stop"